import express from 'express';

const salesRouter = express.Router();

salesRouter.post('/record', (req, res, next) => {});

salesRouter.get('/report', (req, res, next) => {});

export default salesRouter;
